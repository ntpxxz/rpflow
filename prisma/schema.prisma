// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum RequestStatus {
  Pending   @map("pending")   // รอดำเนินการ
  Approved  @map("approved")  // อนุมัติแล้ว (รอสร้าง PO)
  Ordered   @map("ordered")   // สั่งซื้อแล้ว
  Received  @map("received")  // ได้รับของครบแล้ว
  Rejected  @map("rejected")  // ถูกปฏิเสธ
  Cancelled @map("cancelled") // ผู้ขอ Canceled
}

enum ApprovalStatus {
  Pending  @map("pending")
  Approved @map("approved")
  Rejected @map("rejected")
}

enum POStatus {
  Pending   @map("pending")  // ร่าง PO
  Sent      @map("sent")      // ส่ง PO ให้ผู้ขายแล้ว (แม้จะไม่มีตาราง Vendor ก็ตาม)
  Partial   @map("partial")  // ได้รับของบางส่วน
  Fulfilled @map("fulfilled") // ได้รับของครบแล้ว
  Cancelled @map("cancelled")
}



//--- ตารางกระบวนการ (Process Tables) ---

// ตารางผู้ใช้งาน
enum UserRole {
  Requester @map("requester") 
  Approver  @map("approver")  
  Purchaser @map("purchaser")  
  Admin    @map("admin") 
  User  @map("user")
}

model User {
  id              String           @id @default(cuid())
  name            String
  email           String           @unique
  role            UserRole         @default(Requester)
  
purchaseRequests PurchaseRequest[] 
  approvalSteps    ApprovalStep[]    
  historyEntries   RequestHistory[]  
  goodsReceipts    GoodsReceipt[]   
  @@map("users")
}
// (ปรับปรุง) ตารางใบขอซื้อ (ใบหลัก)
enum RequestType {
  NORMAL
  URGENT
  PROJECT
}

enum RequestStatus {
  pending
  approved
  rejected
  ordered
  received
  cancelled
}


model PurchaseRequest {
  id        String   @id 
  status    RequestStatus @default(pending)
  totalAmount Decimal?   @map("total_amount")

  // 2. 🔻🔻 เพิ่ม 2 field นี้ 🔻🔻
  requesterName String
  type          RequestType

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // ความสัมพันธ์
  user    User     @relation(fields: [userId], references: [id])
  userId  String   @map("user_id") // คนที่สร้าง

  items         RequestItem[]
  approvalSteps ApprovalStep[]
  history       RequestHistory[]
  purchaseOrder PurchaseOrder?

  @@map("purchase_requests")
}

//ตารางรายการสินค้าในใบขอซื้อ
model RequestItem {
  id        String   @id @default(cuid())
 
  itemName  String
  detail    String?
  imageUrl  String?
  quantity  Int
  unitPrice Decimal  @db.Decimal(10, 2) // (แนะนำให้ใช้ Decimal สำหรับราคา)

 
  // ความสัมพันธ์กับใบ PR ยังอยู่เหมือนเดิม
  request   PurchaseRequest @relation(fields: [requestId], references: [id])
  requestId String
}

enum ApprovalStatus {
  pending   // 👈 lowercase
  approved  // 👈 lowercase
  rejected  // 👈 lowercase
}

model ApprovalStep {
  id         String         @id @default(cuid())
  stepName   String         @map("step_name")
  status     ApprovalStatus @default(pending) // 👈 เปลี่ยนเป็น lowercase
  comment    String?
  approvedAt DateTime?      @map("approved_at")

  request    PurchaseRequest @relation(fields: [requestId], references: [id], onDelete: Cascade)
  requestId  String          @map("request_id")

  approver   User            @relation(fields: [approverId], references: [id])
  approverId String          @map("approver_id")

  @@unique([requestId, stepName, approverId]) // 👈 เพิ่มเพื่อป้องกันซ้ำ
  @@map("approval_steps")
}
// ตารางประวัติ (เหมือนเดิม)
model RequestHistory {
  id        String   @id @default(cuid())
  action    String
  details   String?
  timestamp DateTime @default(now())

  request   PurchaseRequest @relation(fields: [requestId], references: [id], onDelete: Cascade)
  requestId String          @map("request_id")

  actor     User            @relation(fields: [actorId], references: [id])
  actorId   String          @map("actor_id")

  @@map("request_history")
}

// (ใหม่) ตารางใบสั่งซื้อ (PO)
model PurchaseOrder {
  id       String   @id @default(cuid())
  poNumber String   @unique @map("po_number") // เลขที่ PO
  status   POStatus @default(Pending)
  sentAt   DateTime? @map("sent_at")
  
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // ความสัมพันธ์
  request   PurchaseRequest @relation(fields: [requestId], references: [id])
  requestId String          @unique @map("request_id") // 1 PO มาจาก 1 Request

  items     PurchaseOrderItem[]
  receipts  GoodsReceipt[] // 1 PO อาจมีการทยอยรับของ (หลาย Delivery)

  @@map("purchase_orders")
}

// (ใหม่) ตารางรายการสินค้าใน PO
model PurchaseOrderItem {
  id        String  @id @default(cuid())
  quantity  Int
  unitPrice Decimal @map("unit_price") // ราคาที่ตกลงจริง

  // ความสัมพันธ์
  po     PurchaseOrder @relation(fields: [poId], references: [id], onDelete: Cascade)
  poId   String        @map("po_id")

  itemName String // ชื่อสินค้าตอนสั่งซื้อ
  detail   String? // รายละเอียดตอนสั่งซื้อ
  receiptItems GoodsReceiptItem[]
  @@map("purchase_order_items")
}

// (ใหม่) ตารางการรับของ (Goods Receipt / Delivery Note)
model GoodsReceipt {
  id        String    @id @default(cuid())
  notes     String?   // เช่น "กล่องบุบ", "เลขที่ใบส่งของ XXX"
  receivedAt DateTime @default(now()) @map("received_at")

  // ความสัมพันธ์
  po        PurchaseOrder @relation(fields: [poId], references: [id])
  poId      String        @map("po_id") // 👈 รับของจาก PO ใบไหน

  receivedBy User         @relation(fields: [receivedById], references: [id])
  receivedById String     @map("received_by_id") // 👈 ใครเป็นคนรับของ

  items     GoodsReceiptItem[] // 👈 รายการของที่รับในครั้งนี้

  @@map("goods_receipts")
}

// (ใหม่) ตารางรายการสินค้าที่รับ (Items ใน Delivery)
model GoodsReceiptItem {
  id               String @id @default(cuid())
  quantityReceived Int    @map("quantity_received")

  // ความสัมพันธ์
  receipt          GoodsReceipt      @relation(fields: [receiptId], references: [id], onDelete: Cascade)
  receiptId        String            @map("receipt_id")

  poItem           PurchaseOrderItem @relation(fields: [poItemId], references: [id])
  poItemId         String            @map("po_item_id") // 👈 อ้างอิงว่ารับของรายการไหนจาก PO

  @@map("goods_receipt_items")
}